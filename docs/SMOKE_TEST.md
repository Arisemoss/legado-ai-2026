# 真机冒烟清单（AI 增强版 · 2026 基线移植）

## 安装
1. 打开 Actions 最近一次成功的 `AI Port Build` → Artifacts → 下载 `ai-debug-apk`。
2. 解压得到 `legado_ai_<版本>.apk`（debug 未签名/或 CI 签名取决于 Secrets），执行：
   `adb install -r legado_ai_*.apk`（包名 `io.legado.ai.debug`，可与官方版共存）。

## 配置（首次）
1. 打开 App → 底部「我的」→ **AI 智能助手** → 进入 AI 设置页。
2. 选服务商预设（如 DeepSeek）→ 自动填 Base URL/模型；粘贴 API Key；点 **测试连接** 应显示 ✅。
3. 返回 → 阅读页（任意打开一本书）→ 点右下角 **悬浮球**，或底部菜单 **AI** 项 → 进入 AI Hub。

## 用例
| # | 操作 | 期望 |
|---|------|------|
| 1 | 发送「你好」 | 流式打字机输出（若开启流式），无报错 |
| 2 | 发送「帮我在书源里找《诡秘之主》，并加入书架」 | 触发 `search_books` 工具卡（执行中→完成），返回书名/来源 |
| 3 | 「总结当前正在读的这一章」 | `summarize_chapter`/`read_chapter` 工具卡 + 章节摘要 |
| 4 | 「检测我的书源哪些失效了」 | `test_book_source` 工具卡 + 诊断结果 |
| 5 | 让 AI 移除书架某本书（写操作） | 出现**确认卡**（🔐 待确认）→ 点「同意」才真正移除；点「拒绝」则回填 NO_PERMISSION |
| 6 | Hub 顶栏 🐛 | 打开运行日志页，可见请求/流式/工具/错误记录，支持复制/分享 |
| 7 | 会话管理（顶栏会话按钮） | 新建/切换/删除会话，重启 App 后会话仍在 |

## 故障排查
- 无悬浮球：确认设置里未关闭该功能（当前实现为默认显示；位置记忆键 ai_float_ball_side/ai_float_ball_y_ratio）。
- 工具不触发：检查「工具调用协议」设为 auto（默认）；不支持函数调用的服务商可切 text。
- 正文抓取失败：确认书源可用；桥层对 2026 API 为最小适配，正文走 `WebBook.getContentAwait`。

## 已知限制（本轮）
- AI Hub 入口仅：阅读页悬浮球 / 阅读菜单 AI 项 / 我的页设置入口（无独立桌面图标）。
- 设置页测试连接仅验证 /chat/completions 连通性。
- 写操作白名单：remove_book / suggest_source_fix / set_source_enabled / set_setting。
## 第二批用例（悬浮球 / 模型 / 书源 / 设置）
| # | 操作 | 期望 |
|---|------|------|
| 8 | 回到 App 主页 | 右下角出现 AI 悬浮球；拖拽贴边并记忆位置 |
| 9 | 设置页关闭「显示 AI 悬浮球」→ 返回主页 | 悬浮球消失（重新开启即恢复） |
| 10 | 在设置页粘贴 API Key（Base URL 正确） | 自动出现模型列表（「刷新模型列表」显示数量）；选择后 `ai_model` 更新 |
| 11 | 我的页 →「一键获取书源」 | 列出聚合页条目；选「全部导入」后显示进度与成功/失败统计 |
| 12 | 对 AI 说「帮我导入喵公子书源」 | 出现确认卡（写操作）→ 同意后执行导入并回报条数 |
| 13 | 对 AI 说「看看当前设置」 | 调用 `list_settings`，返回主题/书架/朗读等当前值 |
| 14 | 对 AI 说「把夜间模式打开，书架改成网格」 | 走确认后 `set_setting` 生效（返回 ok） |
| 15 | 对 AI 说「新增一条替换净化：删除广告行」 | 确认后 `manage_replace_rule` 新增规则；用 `list_replace_rules` 可查到 |
| 16 | 失败路径 | 断网时导入/取模型均给出明确失败提示且不崩溃 |

## 第三批用例（导入页 / 向导 / 配置页 / 健康检测）
| # | 操作 | 期望 |
|---|------|------|
| 17 | 我的页 →「一键获取书源」 | 进入导入页：顶部说明 + 列表；每项显示含条数与状态（新书源/可更新/已存在/扫描失败） |
| 18 | 点「仅可更新」→「导入选中」 | 按钮文字随选择变化；导入时显示进度与逐项状态；结束后统计成功/失败 |
| 19 | 导入页点「检测可用性」 | 进入健康检测页；「开始检测」显示进度；结果区分 ✅可用 / ❌失效（含原因与耗时） |
| 20 | 健康页「选中失效项」→「导出备份」→「删除选中」 | 先提示备份；确认后删除并更新列表与统计 |
| 21 | 全新安装/清除数据后首次进入主页 | 自动弹出 AI 配置向导（第 1/4 步）；可「跳过」，且之后不再自动弹出 |
| 22 | 向导：选 DeepSeek → 填 Key → 获取可用模型 | 显示获取到的模型数量并可选择；完成后可直接对话 |
| 23 | 向导第 2 步断网后点获取模型 | 显示失败原因并提示「可跳过，稍后在设置页重试」 |
| 24 | 设置页「AI 智能助手」 | 五组分区清晰；测试连接与刷新模型有内联状态；「工具清单」与「查看运行日志」可点 |

## 第四批用例（P1 工具）
| # | 操作 | 期望 |
|---|------|------|
| 25 | 对 AI 说「把这两本书都加入书架」 | 出现确认卡并列出书名；同意后返回 added/exists/failed 统计 |
| 26 | 对 AI 说「批量检测我的书源」 | `test_sources_batch` 返回 ok/bad 数量与每源耗时/原因（最多 20 源） |
| 27 | 对 AI 说「导入这份净化规则」并给 URL 或 JSON | 确认后导入成功，`list_replace_rules` 可查到 |
| 28 | 对 AI 说「把夜间模式恢复默认」 | 确认后 `reset_setting` 返回 ok |

## 第五批用例（建议动作快捷按钮）
| # | 操作 | 期望 |
|---|------|------|
| 29 | 对 AI 说「帮我在书源里找《诡秘之主》」 | `search_books` 卡片完成后出现按钮「加书架《诡秘之主》」「打开书架」；点「加书架」→ 用户气泡出现带 bookUrl 的指令，并弹出 `add_book_to_shelf` **确认卡** |
| 30 | 点确认卡「同意」 | 卡片回显真实写入结果与耗时，并出现「阅读《…》」「打开书架」；点「阅读」直接进阅读页 |
| 31 | 对 AI 说「看看我书架里有哪些书？」 | `list_shelf` 卡片出现「阅读《X》」（点按直达阅读页）与「打开书架」 |
| 32 | 对 AI 说「批量检测我的书源」 | 有失效源时出现「清理失效书源(N)」→ 点按进入健康检测页；无失效源只出现「打开书源管理」 |
| 33 | 断网状态触发 `search_books` 失败 | 失败卡片**不显示**建议动作（避免引导用户在失败结果上继续操作） |

## 第六批用例（体验修复与 UI 统一）
| # | 操作 | 期望 |
|---|------|------|
| 34 | 清除数据后首启 | 先弹隐私协议；**同意后**才弹 AI 配置向导；向导顶栏与其它 AI 页一致，不压状态栏 |
| 35 | 向导点「跳过」→ 杀进程重进 | 不再自动弹；「我的 → AI 智能助手 → 重新运行配置向导」可重新打开 |
| 36 | 向导：选服务商 → 填 Key → 获取模型 → 完成 | 完成后直接进入 AI Hub；顶栏副标题显示当前模型/服务商 |
| 37 | 向导填 Key 时弹出键盘 | 底部「跳过 / 上一步 / 下一步」不被顶掉（adjustResize + 页面可滚动） |
| 38 | 设置页点「AI 工具清单」 | 弹窗按分类列出全部工具，数量与注册表一致，写操作标注「需确认」 |
| 39 | 切换夜间模式 / 换自定义主题色 | AI Hub、运行日志、向导的顶栏、用户气泡、发送按钮、建议 chip 全部跟随主题；无固定蓝紫残留 |
| 40 | AI Hub 顶栏 | 有返回箭头；状态栏不重叠；会话/新建/日志/设置为图标按钮（无 emoji） |
| 41 | 设置「悬浮球显示范围 = 仅阅读页」→ 回主界面 | 主页/书架/我的不再显示悬浮球；阅读页仍显示 |
| 42 | 我的页「AI 智能助手」图标 | 星芒助手图标（不再是时钟/历史图标） |
| 43 | Hub 空态（未配置模型） | 显示「开始配置模型」按钮，点击进入配置向导 |
| 44 | 我的 →「一键获取书源」/ 检测可用性 | 两个页面都能正常进入（此前 MaterialButton 导致启动即崩） |
| 45 | 阅读页点悬浮球进入 AI Hub | 正常进入不闪退；顶栏有返回箭头 |
| 46 | 用自定义主题色（如棕色）时看 AI 页 | 悬浮球/发送按钮/用户气泡/chip/确认卡按钮与顶栏同色（不再是默认浅蓝） |

## 第七批用例（线程/生命周期修复）
| # | 操作 | 期望 |
|---|------|------|
| 47 | 配置好 Key 后进入「我的 → AI 智能助手」设置页 | 正常打开不闪退（此前 refreshModels 访问未就绪的 viewLifecycleOwner 崩溃） |
| 48 | 设置页点「测试连接」 | 真实发起请求并给出成功/失败（此前必然 NetworkOnMainThreadException 显示连接失败） |
| 49 | 我的 →「一键获取书源」 | 正常读取聚合页列表（此前显示读取失败 NetworkOnMainThreadException）；预扫描与导入均正常 |
| 50 | 向导选择服务商 | 自动进入第 3 步「填写 API Key」；断网时点「获取可用模型」回退预设模型并进入选模型步，可继续完成 |
| 51 | 配置完成后对 AI 发「你好」 | 正常返回（此前首条消息就会因主线程网络崩溃） |

## 第八批用例（审计修复）
| # | 操作 | 期望 |
|---|------|------|
| 52 | 设置页看「API Key」一行 | 不再显示任何 Key 字符，只显示「已配置 · Android Keystore 加密存储」 |
| 53 | Keystore 不可用的设备/模拟回退 | 显示「⚠️ …当前为明文存储」，不再谎称已加密 |
| 54 | AI 运行日志 → 右上角分享 | 先弹警示（含对话内容与工具参数），确认后才进入系统分享 |
| 55 | 让 AI 用 text 协议搜含 `<`/`>` 的书名 | 参数正确解码（不再把 &lt; 原样传给工具） |
| 56 | 第三方 App 访问 readerProvider（默认） | 被拒绝（SecurityException）；在「我的 → 其他设置 → 允许外部 App 访问数据」开启后可正常访问 |
| 57 | 一轮里出现两个写操作 | 两张确认卡都出现，各自可同意/拒绝（不再丢卡或错配） |
| 58 | 把 baseUrl 手改成 http://api.deepseek.com/v1 | 请求被系统拒绝（AI 域名强制 HTTPS）；本地 127.0.0.1 推理仍可用 |

## 第九批用例（真机反馈修复）
| # | 操作 | 期望 |
|---|------|------|
| 59 | 书源可用性检测页点「开始检测」（295 个源） | 进度按总数推进到 100%（不再只测前 50）；失败项自动重试一次；结果显示「检测完成：可用 N · 失效 M（共 X）」 |
| 60 | 检测过程中点「停止检测」 | 立即停止并提示已停止，按钮恢复为「开始检测」 |
| 61 | 用思考模式模型（deepseek-flash / deepseek-reasoner）让 AI 调用工具 | 多轮工具调用不再出现 HTTP 400（reasoning_content 已回传）；运行日志可见 `reasoning=N字` |

## 第十批用例（AI 层自查修复）
| # | 操作 | 期望 |
|---|------|------|
| 62 | 让 AI 执行写操作期间离开 Hub（返回主页等 1 分钟再回来） | 回来仍能看到确认卡并可同意/拒绝（不再丢卡被当拒绝） |
| 63 | 让 AI 一次并行调用多个工具（如「自检一遍」触发 4 个） | Hub 显示 4 张工具卡，各自有执行中→完成状态（不再只显示 1 张） |
| 64 | 流式输出过程中点「停止」 | 显示「已停止」提示，不把半截回答当正常完成 |
| 65 | 主页冷启动观察悬浮球 | 出现在上次记忆的位置（不会跑到屏幕外） |
| 66 | 全新安装、从未进过 AI 设置页 | 默认就是流式输出（与设置页内的开关显示一致） |
| 67 | 删除或清空正在回答的会话 | 任务先停止，不再往已删除会话写消息 |
